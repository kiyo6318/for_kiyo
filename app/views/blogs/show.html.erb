<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @blog.content %>
</p>

<% unless @blog.comments.count.zero? %>
  <hr>

  <p>
    <strong>Comment:</strong>
    <%= @blog.comments.count.to_i + "comments" %>
  </p>

  <p>
    <%= render @blog.comments %>
  </p>
<% end %>

<p>
  <%= render 'comments/form', blog: @blog %>
</p>

<% if false %>
 ErrorCause: path
 FixReason: set the right path baby 
<% end %>

<%= link_to 'Edit', edit_blog_path(@blog.id) %> |
<%= link_to 'Back', blogs_path %>
